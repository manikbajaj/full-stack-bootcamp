// Default export of a function
export default function greet(name) {
  return `Hello, ${name}!`;
}

/*
Explanation:
- The `default` keyword is used to specify the default export of this module.
- Default exports can be imported with any name and don't require curly braces.
*/
